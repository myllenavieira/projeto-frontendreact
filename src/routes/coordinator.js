export const goToHome = (navigate) => {
    navigate('/')
}

export const goToProducts = (navigate) => {
    navigate(`/cart`)
}

export const goToContacts = (navigate) => {
    navigate(`/contact`)
}